# Copy to .env and adjust. The .env file is gitignored.
DATABASE_URL="postgresql://USER@localhost:5432/inhology?schema=public"

# Bearer token required for write endpoints (POST/PATCH/DELETE).
# Generate one with: openssl rand -hex 32
ADMIN_TOKEN="change-me"
